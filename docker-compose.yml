version: "3.9"
services:
  redis:
    image: redis
    ports:
      - 6379:6379

  postgres:
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}

